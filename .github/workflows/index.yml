name: Automated Testing
on: 
   push:
        branches: ['main']
jobs:
        Testing:
              runs-on: ubuntu-latest
              steps:
                      - name: Unit Testing
                        run: echo "Testibg index.html file"
